{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "io.github.leonardosepulvedat/mcp-n8n",
  "description": "Operate and build n8n: 61 tools with a full 560-node catalog (real schemas), validation, autofix, surgical edits, snapshots with rollback and diff, per-node execution debugging, health reports, full-instance backup, webhook testing, and remote HTTP mode.",
  "repository": {
    "url": "https://github.com/leonardosepulvedat/mcp-n8n",
    "source": "github"
  },
  "version": "1.5.0",
  "packages": [
    {
      "registryType": "npm",
      "registryBaseUrl": "https://registry.npmjs.org",
      "identifier": "mcp-n8n",
      "version": "1.5.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "N8N_BASE_URL",
          "description": "Base URL of the n8n instance (e.g. https://your-n8n-instance.com)",
          "isRequired": true,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "N8N_API_KEY",
          "description": "n8n API key (Settings -> n8n API)",
          "isRequired": true,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "N8N_TOOLSETS",
          "description": "Comma-separated toolsets to expose: core, builder, admin (default: all)",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    }
  ]
}